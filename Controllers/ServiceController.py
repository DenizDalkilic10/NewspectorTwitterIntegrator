# This class uses TwitterServices.py methods for retrieving tweet info and setting them to the models
# Then by using the information kept in model classes, it uploads the information fetched to Firestore database
# The fetching and uploading operations should be done in every xxx minutes and each time new tweets should be selected

